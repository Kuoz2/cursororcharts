
<nav class=" flex w-full flex-col bg-black items-center p-5 justify-between fixed">
    <div class="flex gap-5 items-center">
        <%= image_tag('venta.png', class: 'w-8')%>
        <h3 class="font-bold text-lg text-white">Los libros a la venta</h3>
    </div>

    <div class="flex ">
    <%= turbo_frame_tag 'new_record_component', class:'flex gap-2' do %>
    <%= link_to new_sell_path, id: 'sell-link', class:'text-white font-semibold text-sm text-center bg-green-700 rounded px-4 py-2' do  %>
        Add sell
        <% end %>
    <%= link_to new_book_path, id: 'book-link', class:'text-white font-semibold text-sm text-center  rounded  bg-sky-700 px-4 py-2' do %>
        Add a new book
        <% end %>
    <%end%>
    </div>
</nav>
